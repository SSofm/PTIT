#include<bits/stdc++.h>
#define int long long 
using namespace std;

int32_t main(){
    string s;   cin >> s;
    stack<char> cnt;
    for(int i = 0 ; i < s.size(); i++){
        if(s[i] == '<'){
            while(!cnt.empty()){

            }
        }
        cnt.push(s[i]);
    }
    return 0;
}